## what
- replaced ppd (power-profile-daemon) with tlp, and install bash-completion (optional for tlp), but needed for myself.
- started and enabled it on every boot

## why
play with tlp to see how things goes

## Command
sudo pacman -Rcns power-profile-daemon
sudo pacman -S tlp bash-completion

sudo tlp start
sudo systemctl enable tlp.service

## Output
