## what
enabled NetworkManager for nm-applet on startup

## why
nm-applet requires NetworkManager running to manage network connections

## Command
`sudo systemctl enable NetworkManager`
`sudo systemctl start NetworkManager`

## Output
no errors; service enabled and started
